# Current Concord runtime checkpoint

Observed `2026-08-14T18:37:32Z`. This is the latest hosted-runtime evidence;
it supersedes the earlier deployment-attempt notes that describe the
Workers.dev relay as backed by a stopped Codespace.

## Verified now

- Northflank project: `concord`, region `europe-west`.
- Proxy service: `concord-fcc-proxy`, build `SUCCESS`, deployment `COMPLETED`.
- Simulated TEE service: `concord-fcc-tee`, build `SUCCESS`, deployment
  `COMPLETED`.
- Private Redis addon: `concord-redis`, status `running`.
- Proxy readiness and liveness: private `GET /healthy` on port `6663`.
- Public proxy port: `6664`, HTTPS hostname
  `pub6664--concord-fcc-proxy--n4krppffn8ms.code.run`.
- Cloudflare relay:
  `https://concord-fcc-ingress.microcosm.workers.dev`.
- Direct and relayed `/info`: HTTP `200`, extension `66188`, Coston2 chain ID
  `114`, matching code/governance/platform bindings, and a proxy signature.

The local sandbox's native resolver still cannot resolve the public hostnames;
the configured HTTPS egress reaches both public URLs and is the basis for this
checkpoint. That local DNS limitation is not presented as a hosted-runtime
health result.

The Worker is a development relay to Northflank. It is not a named Cloudflare
Tunnel and does not establish production hosting or hardware-backed TEE
security.

## Current simulated identity

The live `/info` public key currently derives to:

```text
0xb1e7a4c1930f1f3c4905b34fafc9c1b8359029a5
```

This identity is generated by the simulated TEE process and is ephemeral. It
is not a substitute for an onchain registration record. At this checkpoint no
Coston2 registration, availability, pause, dispatch, or other broadcast was
performed for this Northflank identity.

Read-only Systems Explorer inspection found these pre-existing records for the
same owner and extension:

| TEE ID | Status | Interpretation |
|---|---|---|
| `0x65721B35EAF2648Fd061aB6901e1355ec2eCffd2` | `INITIALIZED` | Pre-existing, not active |
| `0xeE39d5e7d1C5043232282e3CC884B41a9Db22c85` | `PRODUCTION` | Pre-existing active record |

The current identity must be registered before it can be presented as the
active Northflank machine. The stale `PRODUCTION` record must be paused only
after the replacement reaches `PRODUCTION`; the `INITIALIZED` record is left
untouched unless an operator explicitly decides otherwise.

## Judge checks

From a clean public checkout:

```bash
./scripts/judge-check.sh
```

This verifies the recorded Coston2 evidence, local documentation gates, and
the public `/info` binding without credentials or transaction authority. During
an agreed live window, a runner with Go can add the read-only registry query:

```bash
./scripts/judge-check.sh --registry
```

The optional registry mode never signs or broadcasts. It only passes once the
current hosted identity has been registered, has a fresh availability proof,
and exactly one active machine remains for extension `66188`.

## Claims boundary

The completed Coston2 economic proof remains the canonical submission proof:
Root Accord, FCC/CoFill result, funded Child Accords, multi-provider draw,
repayment, and restored capacity. The hosted Northflank runtime is a current
development-path availability checkpoint. Do not claim private token
transfers, private EVM execution, continuous production availability, or
hardware-backed TEE security from it.
